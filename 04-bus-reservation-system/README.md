# Bus Reservation System

Java + MySQL/JDBC desktop reservation prototype.

## Features
- Bus schedule storage
- Passenger reservation
- Seat availability check
- Reservation records
- Transaction-safe booking logic

## Setup
Import `schema.sql`, update DB credentials in `BusReservationSystem.java`, then compile:
`javac -cp mysql-connector-j.jar BusReservationSystem.java`
